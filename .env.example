# URL base da API IGNIS Orbital (sem barra final)
VITE_API_URL=http://localhost:8000

# Ativa integração com a API real (true) ou usa mocks locais (false)
# Mantenha false em desenvolvimento se a API não estiver rodando
VITE_USE_API=false
